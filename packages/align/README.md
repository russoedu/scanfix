# align

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build align` to build the library.

## Running unit tests

Run `nx test align` to execute the unit tests via [Vitest](https://vitest.dev/).
