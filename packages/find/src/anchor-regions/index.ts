/** Regions placed relative to text the original prints - a signature box from its label. */

export { checkRegions, locateAnchor, regionsFromAnchor, resolveRegions } from './anchor-regions.use-case'
export type { AnchorOptions, FieldOffset, RegionProblem, RegionSpec, ResolvedRegions } from './anchor-regions.use-case'
