/** Finding a short text inside a long one, allowing for OCR's errors - and none in figures. */

export { approximateSearch, bestMatch, wordSpan } from './approximate-search.use-case'
export type { ApproximateMatch, SearchOptions } from './approximate-search.use-case'
